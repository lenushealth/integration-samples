@model ErrorViewModel

<div class="alert alert-danger">
    <h4>Unable to retrieve data</h4>
    <p>An error occurred : @Model.Exception.Message</p>
    @if (Model.ShowRequestId)
    {
        <p class="text-muted">@Model.RequestId</p>
    }
</div>